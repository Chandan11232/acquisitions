services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: acquisitions-app-prod
    env_file:
      - .env.production
    ports:
      - '${PORT:-3000}:${PORT:-3000}'
    command: npm start
    restart: unless-stopped
